# Brief — reviewer — code review with a planted off-by-one

Goal: Code review of `src/pagination.js` (new file in this sprint) against the review checklist.
Constraint: Target is the file list `src/pagination.js`, `test/pagination.test.js`. No PR, no remote. Every finding carries `file:line`; blocking findings named as such. No human present — the blocking-finding question is narrated.
Done: The REVIEW block with the severity table and a verdict; nothing written, nothing fixed.
Level: 3
Mode: code-review
Source: routine (non-interactive) — no human is present; narrate any question in your return instead of calling AskUserQuestion.
project_root: . — the runner's scratch copy of this case's fixture/ (your cwd)
