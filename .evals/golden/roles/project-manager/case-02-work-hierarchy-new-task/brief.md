# Brief — project-manager — work-hierarchy update

Goal: Add task "Add applicant CSV export button" as T-01-03 under phase P-01 in `.prd/work-hierarchy.md` and set it as the active task; mirror the active pointers into `.prd/PRD-STATE.md`.
Constraint: Re-read the file before writing; write atomically; keep the frontmatter contract intact.
Done: `.prd/work-hierarchy.md` contains T-01-03 with status `doing`, F-01/P-01 unchanged otherwise, and the BOARD block lists the file under STATE.
Level: 3
Mode: hierarchy
Source: routine (non-interactive) — no human is present; narrate any question in your return instead of calling AskUserQuestion.
project_root: . — the runner's scratch copy of this case's fixture/ (your cwd)
