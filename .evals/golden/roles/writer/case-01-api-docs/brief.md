# Brief — writer — API docs from the codebase

Goal: Docs mode, scope `api`, for this repo.
Constraint: Every endpoint documented exists in `src/routes/`; real request and response examples from the code; the generated-file marker and date on every file. Omit a section before inventing one.
Done: `docs/api/README.md` and `docs/api/endpoints/*.md` written; the WRITER block lists them.
Level: 1
Mode: docs
scope: api
Source: routine (non-interactive) — no human is present; narrate any question in your return instead of calling AskUserQuestion.
project_root: . — the runner's scratch copy of this case's fixture/ (your cwd)
