# North Star — {quarter}

The chief of staff reads this before anything else. Every DROP it makes cites a goal
here; every ACT names the goal it advances. Without this file, its triage is taste.

Keep it short enough to stay true. A North Star nobody updates is worse than none,
because the agent will keep enforcing a quarter that already ended.

## Goals

At most three. If there is a fourth, it is next quarter's.

1. **{goal}** — done when {observable condition}. Matters because {consequence of missing it}.
2. **{goal}** — done when {observable condition}. Matters because {consequence of missing it}.
3. **{goal}** — done when {observable condition}. Matters because {consequence of missing it}.

## Not this quarter

Name what you are deliberately not doing. This is the half that makes DROP defensible —
without it the agent has to infer your refusals, and it will infer them wrong.

- {thing} — revisit {when}
- {thing} — revisit {when}

## Fixed constraints

Things the agent must treat as immovable rather than as problems to solve.

- {constraint, e.g. a hard budget, a compliance rule, a platform decision already made}

## Review

Last updated: {date}. Review on {date}.

If today is past the review date, the chief of staff says so in its brief and treats
these goals as provisional rather than binding.
