# RAID Log
# Generated by /cks:control-plane init — do not delete this file.
# Append new entries at the bottom. Never edit or delete existing entries.
# Agents grep for "^Type:" and "^Status: Open" — keep fields on their own lines.
#
# Severity: Low | Medium | High | Critical
# Status:   Open | Mitigated | Resolved | Accepted
# Type:     Risk | Assumption | Issue | Dependency
# Owner:    [agent-id] | human

---

## [YYYY-MM-DD] Risk: Example — replace or delete this entry
Type: Risk
Title: Example RAID entry — replace with a real item
Severity: Low
Status: Accepted
Owner: human
Details: This is a placeholder entry created by the init script. Replace it with real RAID items as your project evolves.
Mitigation: —
Resolution: Accepted as placeholder
Linked: —
Opened: YYYY-MM-DD
Closed: —

---

# HOW TO ADD ENTRIES
#
# 1. Append a new block at the bottom of this file (above this comment section is fine).
# 2. Copy the format above: separator line, heading, then all fields each on its own line.
# 3. Set Status: Open — agents grep for this to list active items.
# 4. When closing an item, change Status and fill in Resolution + Closed date.
#
# GREP PATTERNS (used by agents — do not break this format):
#   Open items:    grep "^Status: Open" .cks/control-plane/raid/raid.md
#   By type:       grep "^Type: Risk" .cks/control-plane/raid/raid.md
#   By severity:   grep "^Severity: High" .cks/control-plane/raid/raid.md
#
# EXAMPLE ENTRIES:
#
# ---
# ## [2026-05-20] Risk: API rate limit under load
# Type: Risk
# Title: Third-party API may rate-limit under sustained load
# Severity: High
# Status: Open
# Owner: sre-lead
# Details: The payment API allows 100 req/s. Our peak load estimate is 80 req/s with no headroom.
# Mitigation: Add retry with exponential backoff; monitor rate-limit headers in production.
# Resolution: —
# Linked: goal-002
# Opened: 2026-05-20
# Closed: —
#
# ---
# ## [2026-05-20] Assumption: SSO provider supports PKCE
# Type: Assumption
# Title: Auth provider supports PKCE for mobile clients
# Severity: Medium
# Status: Open
# Owner: principal-engineer
# Details: Auth design assumes PKCE flow. Verify before Sprint 3.
# Mitigation: Check provider docs; fall back to client-credentials if not supported.
# Resolution: —
# Linked: —
# Opened: 2026-05-20
# Closed: —
