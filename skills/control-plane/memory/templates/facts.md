# Project Facts
<!-- Append-only. Each entry: ## [YYYY-MM-DD] Short Title -->
<!-- Facts: immutable truths about the project (tech stack, constraints, integrations) -->

## [YYYY-MM-DD] Example Fact
The project uses Supabase for all persistent storage. The `supabase_url` and `supabase_service_key` are stored in `.cks/control-plane/config.yaml` and never committed to git.
